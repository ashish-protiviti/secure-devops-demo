//write Jest tests for POST /api/leave/request — success, insufficient balance, missing auth, validation errors
function generateToken() {
  // Mock function to generate a valid JWT token for testing
  return 'mocked-jwt-token';
} 